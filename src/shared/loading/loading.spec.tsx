import React from 'react';
	import { Enzyme, Wrapper } from 'reactive';
	import ExampleComponent from './example.component';

	it('test - ', () => {
	  const wrapper: Wrapper = Enzyme.mount(
		
	  );

	  wrapper.unmount();
	});
  